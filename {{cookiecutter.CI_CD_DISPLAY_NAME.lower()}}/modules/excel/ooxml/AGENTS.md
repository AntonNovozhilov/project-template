# modules/excel/ooxml

- Используй пакет только для низкоуровневой работы с XLSX как OOXML-архивом.
- Перед изменением ZIP/XML-контента фиксируй контракт листов, стилей,
  sharedStrings, формул и пересчёта.
- Для обычного чтения и записи через openpyxl используй `readers` и `writers`.
