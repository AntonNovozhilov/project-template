[project]
name = "{{ cookiecutter.CI_CD_DISPLAY_NAME.lower() }}"
version = "0.1.0"
description = "{{ cookiecutter.CI_CD_DESCRIPTION }}"
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
pythonpath = ["."]
