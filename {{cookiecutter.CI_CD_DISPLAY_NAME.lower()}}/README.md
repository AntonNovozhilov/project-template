# {{ cookiecutter.project_name }}

Номер заявки: `{{ cookiecutter.CI_CD_DISPLAY_NAME }}`

Описание: {{ cookiecutter.CI_CD_DESCRIPTION }}

Автор: `{{ cookiecutter.CI_CD_AUTHOR }}`

<!--
Кратко опишите бизнес-задачу.
README должен позволить человеку, незнакомому с проектом,
понять назначение приложения без чтения исходного кода.
-->

## Входные данные

<!--
Если в проекте необходимо использовать какие то шаблоны или переиспользуемые
файлы, то их необходимо сохранить в папке `./modules/assets`.
После инициализации проекта они будут скопированы в папку `/home/.rpa/`

Опишите входные данные приложения:
- типы и форматы файлов;
- обязательные поля / листы / столбцы;
- откуда берутся данные;
- допустимые варианты входных данных.

Пример:
- график платежей — Excel (.xlsx) или Word (.docx);
- реестр договоров R12 — Excel (.xlsx).
-->

- ...

## Результат

<!--
Опишите результат работы приложения:
- создаваемые файлы;
- изменяемые файлы;
- куда сохраняется результат;
- основные поля результата.

Пример:
Формируется Excel-файл `result.xlsx`, содержащий:
- номер договора;
- системный номер;
- сумму с НДС;
- сумму без НДС;
- НДС.
-->

- ...

## Основные бизнес-правила

<!--
Опишите только ключевые правила, без деталей реализации.

Пример:
- номер договора нормализуется перед поиском;
- системный номер договора определяется по реестру R12;
- суммы разделяются на "с НДС", "без НДС" и "НДС".
-->

- ...

## Ограничения

<!--
Опишите известные и сознательно принятые ограничения приложения.

Пример:
- поддерживаются только файлы .xlsx и .docx;
- один входной файл должен относиться к одному договору;
- договор должен присутствовать в реестре R12.
-->

- ...

## Требования

- Python 3.9
- `uv`

Зависимости проекта описаны в `pyproject.toml` и зафиксированы в `uv.lock`.

### Установка зависимостей

```bash
uv sync
```

## Запуск

```bash
uv run python -m {{cookiecutter.project_slug.lower()}} [опции]
```

### Возможные опции

<!--
Удалите раздел, если аргументы командной строки отсутствуют.

Пример:
- `--input PATH` — путь к входному файлу;
- `--output PATH` — каталог для результата.
-->

- ...

### Конфигурация

<!--
Удалите раздел, если дополнительная конфигурация отсутствует.

Опишите:
- конфигурационные файлы;
- переменные окружения;
- обязательные настройки.

Не указывайте здесь реальные пароли, токены и другие секреты.
-->

...

## Повторный запуск

<!--
Опишите последствия повторного запуска приложения.

Особенно важно указать:
- изменяются ли исходные файлы;
- перезаписывается ли результат;
- безопасно ли повторно запустить приложение после ошибки;
- требуется ли предварительная очистка файлов/каталогов.
-->

...

## Логи

Логи работы приложения сохраняются в каталоге `logs/`.

Для каждого запуска создаётся отдельный файл журнала.
Хранятся последние несколько запусков приложения.

<!--
При необходимости укажите дополнительную информацию:
- что попадает в лог;
- какие данные намеренно не логируются;
- где искать информацию при ошибке.
-->

## Архитектура и структура

<!--
Дайте минимальное описание только основных компонентов проекта.
Не документируйте здесь каждый файл и класс.
-->

Основные компоненты:

- `{{cookiecutter.project_slug.lower()}}/` — код приложения;
- `{{cookiecutter.project_slug.lower()}}/__main__.py` — точка входа;
- `tests/` — автоматические тесты.

## Тестирование

Запуск тестов:

```bash
uv run pytest
```

## Проверка качества кода

Проверка форматирования:

```bash
uv run black --check .
```

Статический анализ:

```bash
uv run pylint {{cookiecutter.project_slug.lower()}}
```
